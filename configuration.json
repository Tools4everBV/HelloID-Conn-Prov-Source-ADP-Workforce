[
  {
    "key": "BaseUrl",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "label": "BaseUrl",
      "placeholder": "https://test-api.adp.com",
      "description": "The BaseUrl to the ADP Workforce environment",
      "required": true
    }
  },
  {
    "key": "ClientID",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "label": "ClientID",
      "placeholder": "DEX000",
      "description": "The ClientID for the ADP Workforce environment. This will be provided by ADP",
      "required": true
    }
  },
  {
    "key": "ClientSecret",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "label": "ClientSecret",
      "type": "password",
      "placeholder": "0000-0000-0000-0000",
      "description": "The ClientSecret for the ADP Workforce environment. This will be provided by ADP",
      "required": true
    }
  },
  {
    "key": "Certificate",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "label": "Certificate",
      "placeholder": "C:\\ADP.pfx",
      "description": "The location to the 'private key of the x.509 certificate' on the server where the HelloID agent and provisioning agent are running. Make sure to use the private key for the certificate that's used to generate a ClientID and ClientSecret and for activating the required API's",
      "required": true
    }
  },
  {
    "key": "ProxyServer",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "label": "ProxyServer",
      "placeholder": "http://localhost:8888",
      "description": "The URL (or IP Address) to the ProxyServer in the network. Leave empty if no ProxyServer is being used",
      "required": true
    }
  },
  {
    "key": "ApiScope",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "label": "ApiScope",
      "placeholder": "api",
      "description": "The name of the API you need access to. For instance, 'worker-demographics'. You can specficy more API's by separating them with a comma. To get access to all available API's, set the scope to: 'api'",
      "required": true
    }
  },
  {
    "key": "ImportFile",
    "type": "checkbox",
    "defaultValue": false,
    "templateOptions": {
      "label": "Import from JSON file (For testing purposes only)",
      "description": "Use this in combination with the JSON file to test the import of the workers without making API calls to ADP Workforce",
      "required": false
    }
  },
  {
    "key": "JsonFile",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "label": "JsonFIle (For testing purposes only)",
      "placeholder": "C:\\worker-demographics.json",
      "description": "The location to the 'JSON file containing the workers' on the server where the HelloID agent and provisioning agent are running.",
      "required": false
    }
  }
]
